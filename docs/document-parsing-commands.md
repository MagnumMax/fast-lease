# Команды для запуска парсинга папок документов

## ⚠️ ВАЖНО: Настройка переменных окружения

Перед запуском команд необходимо создать файл `.env.local` в корне проекта:

### 1. Создайте файл .env.local:
```bash
# В корне проекта создайте файл .env.local
# Скопируйте содержимое из .env.example или создайте новый
```

### 2. Заполните .env.local своими ключами:
```bash
# FastLease Environment Variables
GEMINI_API_KEY=ваш_gemini_api_key_here
NEXT_PUBLIC_SUPABASE_URL=ваш_supabase_url_here
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key_here
```

**Получение API ключей:**
- `GEMINI_API_KEY`: [Google AI Studio](https://makersuite.google.com/app/apikey)
- `NEXT_PUBLIC_SUPABASE_URL` и `SUPABASE_SERVICE_ROLE_KEY`: Панель Supabase → Settings → API

## 🔧 Ключевые настройки конфигурации

В файле `configs/drive_ingest.yaml` настроены следующие параметры:
### ⚠️ ВНИМАНИЕ: Миграция не нужна!

### ✅ МИГРАЦИЯ УСПЕШНО ЗАВЕРШЕНА!

**Результат:** Вся структура Storage успешно мигрирована из `deals/documents/{deal_id}/` в плоскую структуру `{deal_id}/`.

**Текущая структура:**
```
deals (бакет)
├── 016f4d12-4a35-596e-b5fe-905e22a83219/
│   ├── документы.pdf
│   ├── анализ.json
│   └── aggregated.json
├── 02805b45-ec9d-5fe8-b4c7-fedab78d2377/
│   └── ...
└── ... (24 сделки)
```

**Статистика миграции:** 527 файлов, 24 папки - ВСЕ ПЕРЕМЕЩЕНО ✅

### Миграция в плоскую структуру Storage
```bash
npm run migrate:storage:flat
```
или
```bash
node scripts/migrate_to_flat_structure.mjs
```

**Статус:** ✅ Выполнено - файлов для миграции не найдено, структура уже правильная.

### Очистка дублированной структуры Storage
```bash
npm run cleanup:storage
```
или
```bash
node scripts/cleanup_storage_structure.mjs
```

**Что делает:** Ищет и перемещает файлы из дублированной структуры `deals/deals/documents/` в правильную `documents/`.

**Проверка через MCP сервер показала:** Все файлы уже находятся в правильной структуре `deals/documents/`. Дублированных файлов в `deals/deals/` нет.

**Команда миграции больше не требуется** - система уже работает корректно!

### Миграция файлов в Storage (одноразовая)
```bash
npm run migrate:storage
```
или
```bash
node scripts/ingest_local_deals.mjs --migrate-storage
```

**ВАЖНО:** Запустите эту команду ОДИН РАЗ перед основным парсингом, если у вас есть дублированные папки в Storage (deals/deals/documents → deals/documents).
- `skip_processed: true` - пропускает уже обработанные папки
- `storage_prefix: "documents"` - избегает дублирования пути "deals/deals"
**Автоматическая загрузка:** Скрипт автоматически загрузит переменные из `.env.local` при запуске.
## 🚀 Основная команда

### Полный анализ всех папок с загрузкой в Supabase Storage
```bash
npm run ingest:deals
```
или
```bash
node scripts/ingest_local_deals.mjs
```

**Что делает:** 
- ✅ Анализирует все PDF документы в папке `datasets/deals`
- ✅ Извлекает данные с помощью Gemini AI
- ✅ Загружает PDF и JSON файлы в Supabase Storage
- ❌ НЕ создает записи в таблицах базы данных (clients, vehicles, deals)

### Анализ конкретных папок
```bash
npm run ingest:deals:specific -- "НазваниеПапки1,НазваниеПапки2"
```
или
```bash
node scripts/ingest_local_deals.mjs --only "НазваниеПапки1,НазваниеПапки2"
```

**Пример:**
```bash
npm run ingest:deals:specific -- "deal_001,deal_002"
```

### Анализ из другой папки
```bash
npm run ingest:deals:root -- "/путь/к/папке/с/документами"
```
или
```bash
node scripts/ingest_local_deals.mjs --root "/путь/к/папке/с/документами"
```

**Пример:**
```bash
npm run ingest:deals:root -- "/Users/maksymholovakhin/Desktop/deals"
```

## Дополнительные команды

### Проверка структуры папок
```bash
npm run ingest:check
```
или
```bash
node scripts/check_deals_folders.mjs
```

### Получение отчета о результатах
```bash
npm run ingest:report
```
или
```bash
node scripts/final_deals_report.mjs
```

## Использование собственной конфигурации

### Указание кастомного конфигурационного файла
```bash
node scripts/ingest_local_deals.mjs --config configs/my_custom_config.yaml
```

## Комбинирование параметров

Можно комбинировать несколько параметров:

```bash
node scripts/ingest_local_deals.mjs \
  --config configs/drive_ingest.yaml \
  --root "/Users/maksymholovakhin/Desktop/deals" \
  --only "deal_001,deal_002,deal_003"
```

## Предварительные требования

### Переменные окружения
Убедитесь, что создан файл `.env.local` с переменными:

```bash
GEMINI_API_KEY=ваш_gemini_api_key
NEXT_PUBLIC_SUPABASE_URL=ваш_supabase_url
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key
```

### Структура папок
Ожидаемая структура:
```
datasets/deals/
├── deal_001/
│   ├── document1.pdf
│   ├── document2.pdf
│   └── ...
├── deal_002/
│   ├── contract.pdf
│   └── ...
└── ...
```

## Процесс работы

1. **Сканирование папок:** Находит все PDF файлы в указанной директории
2. **Анализ документов:** Каждый PDF анализируется с помощью Gemini AI
3. **Извлечение данных:** Извлекается информация о клиентах, автомобилях, сделках
4. **Агрегация:** Данные из всех документов объединяются в единую структуру
5. **Загрузка в Storage:** Результаты загружаются в Supabase Storage (НО НЕ в таблицы БД)

## Выходные данные

После завершения в Supabase Storage создаются:
- Отдельные JSON файлы для каждого документа
- Агрегированный JSON файл для всей сделки (`aggregated.json`)
- PDF файлы в оригинальном виде

**ВАЖНО:** Данные НЕ записываются в таблицы БД (clients, vehicles, deals). Файлы только загружаются в Storage.

## Логирование

Скрипт предоставляет подробное логирование:
- 📁 Найденные папки и файлы
- 🔍 Обработанные документы
- ✅ Успешные операции
- ❌ Ошибки и проблемы
- 📊 Статистика обработки
- 📋 Завершение процесса

## Пример использования

```bash
# 1. Проверим структуру папок
npm run ingest:check

# 2. Запустим анализ всех папок (загрузка в Storage только)
npm run ingest:deals

# 3. Или анализ конкретных папок
npm run ingest:deals:specific -- "deal_001,deal_002"

# 4. Получим отчет о результатах
npm run ingest:report
```

## Различия в процессе

| Функция | Текущий скрипт | Дополнительный импорт |
|---------|---------------|----------------------|
| Анализ PDF через Gemini AI | ✅ | ✅ |
| Извлечение данных | ✅ | ✅ |
| Агрегация результатов | ✅ | ✅ |
| Загрузка в Supabase Storage | ✅ | ✅ |
| Запись в таблицы БД | ❌ | ✅ |
| Скорость выполнения | ⚡ Быстрее | 🐌 Медленнее |
| Использование квот API | 💸 Использует | 💸 Использует |

## Структура файлов в Storage

После выполнения команды в Supabase Storage (bucket: `deals`) создается следующая структура:

```
deals/documents/<deal_id>/
├── document1.pdf           # PDF файлы
├── document1.json          # Анализ отдельного документа
├── document2.pdf
├── document2.json
├── ...
└── aggregated.json         # Агрегированные данные всей сделки
```

Каждый `aggregated.json` содержит полную структурированную информацию о сделке, но НЕ создает соответствующие записи в таблицах БД.
# Диагностический отчет: Ошибка "Database error querying schema"

## 🔍 Результаты диагностики

### Источник проблемы
Ошибка "Database error querying schema" вызвана **отсутствующими RPC функциями** в базе данных Supabase.

### Корень причины
Код в `lib/supabase/admin-auth.ts` использует **3 RPC функции**, но в базе данных существует только **1**:

#### ✅ Существующая функция:
- `get_auth_user_by_email` - найдена в `migrations/20251112091500_create_get_auth_user_by_email_function.sql`

#### ❌ Отсутствующие функции:
- `count_auth_users` - используется в строках 143, 144
- `get_all_auth_users` - используется в строках 149, 151

### Места использования
Отсутствующие RPC функции используются в:
1. **`app/api/admin/users/create/route.ts`** - строка 13, функция `findSupabaseAuthUserByEmail`
2. **Внутренние функции admin-auth.ts** - для получения списка и количества пользователей

### Последствия
- Ошибка возникает при попытке доступа к admin функциям
- Невозможно создать пользователей через admin API
- Логика пагинации пользователей не работает

## 🛠️ Исправление проблемы

### 1. Создан SQL скрипт
Создан файл **`fix-database-schema-error.sql`** с недостающими RPC функциями:

```sql
-- count_auth_users: Подсчет общего количества пользователей
create or replace function public.count_auth_users()
returns bigint
language sql
security definer
set search_path = auth
as $$
  select count(*)::bigint from auth.users;
$$;

-- get_all_auth_users: Пагинированный список пользователей
create or replace function public.get_all_auth_users(
  page_number int default 1,
  page_size int default 30
)
returns table (...)
```

### 2. Улучшенное логирование
Добавлено детальное логирование в `lib/supabase/admin-auth.ts`:
- Логирование вызовов RPC функций
- Детальные сообщения об ошибках
- Трассировка результатов запросов

### 3. Миграции
Созданы файлы миграций:
- `supabase/migrations/20251113220000_create_missing_auth_functions.sql`
- `migrations/20251113220000_create_missing_auth_functions.sql`

## 📋 Инструкции по применению исправления

### Вариант 1: SQL Editor в Supabase Dashboard
1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в "SQL Editor"
4. Скопируйте и выполните содержимое файла `fix-database-schema-error.sql`

### Вариант 2: Supabase CLI
```bash
cd /Users/maksymholovakhin/fast-lease
supabase db reset
```

### Вариант 3: Применение миграции
```bash
supabase db push --linked
```

## ✅ Результат исправления

После применения исправления:
- RPC функции `count_auth_users` и `get_all_auth_users` будут созданы
- Admin API эндпоинты начнут работать корректно
- Ошибка "Database error querying schema" исчезнет
- Логирование поможет отслеживать работу функций

## 🔧 Техническая информация

### Безопасность
Все RPC функции созданы с:
- `SECURITY DEFINER` для доступа к auth схеме
- `SET search_path = auth` для изоляции
- Правильными правами доступа только для service_role и supabase_auth_admin

### Функции работают с таблицей
- `auth.users` - системная таблица Supabase для хранения пользователей
- Обеспечивают безопасный доступ к метаданным пользователей

### Совместимость
Исправления совместимы с существующим кодом и не требуют изменений в приложении.